##name: CI/CD with Cortex

#on:
  push:
    branches:
      - main

#jobs:
 # build-test-scan:
    #runs-on: ubuntu-latest

    #steps:
      # 1. Checkout
      #- name: Checkout code
      #  uses: actions/checkout@v3

      # 2. Node setup
      #- name: Setup Node.js
      #  uses: actions/setup-node@v3
        #with:
         # node-version: '18'

      # 3. Install deps
      #- name: Install dependencies
       # run: npm install

      # 4. Run tests
      #- name: Run tests
       # run: npm test

      # 5. Build Docker image
      #- name: Build Docker image
       # run: docker build -t demo-cicd-app:latest .

      # 6. Install Cortex CLI (FIXED)
      ## run: |
        #  sudo apt install jq -y
         # crtx_resp=$(curl -s "https://api-atpl-nfr.xdr.in.paloaltonetworks.com/public_api/v1/unified-cli/releases/download-link?os=linux&architecture=amd64" -H "x-xdr-auth-id: 45" -H "Authorization: NrQivSxdJwfgerY61f61cErkGpUA1dAUECToHiU2xsxfCfjiRmDEGraLFh5dHOSTYfsgyzuNSehmS2uKSpzM3i7CAZ3RMWTVfXHR6Jp8kGdgH9jbqKb83kKbhfW5TeLZ") && crtx_url=$(echo $crtx_resp | jq -r ".signed_url") && crtx_file=$(echo $crtx_resp | jq -r ".file_name") && curl -o $crtx_file $crtx_url 
          #chmod +x cortexcli
         # curl -k -u 45::NrQivSxdJwfgerY61f61cErkGpUA1dAUECToHiU2xsxfCfjiRmDEGraLFh5dHOSTYfsgyzuNSehmS2uKSpzM3i7CAZ3RMWTVfXHR6Jp8kGdgH9jbqKb83kKbhfW5TeLZ --output ./cortexcli https://api-atpl-nfr.xdr.in.paloaltonetworks.com/api/v2/remote-li/{version}/{platform}/artifacts
         # chmod +x cortexcli
         # ls -la

      # 7. Verify installation (DEBUG step)
      #- name: Verify Cortex CLI
      #  run: ./cortexcli -v

      # 8. Login using secret
      #- name: Cortex Login
      #  run: ./cortexcli --api-base-url https://api-atpl-nfr.xdr.in.paloaltonetworks.com --api-key NrQivSxdJwfgerY61f61cErkGpUA1dAUECToHiU2xsxfCfjiRmDEGraLFh5dHOSTYfsgyzuNSehmS2uKSpzM3i7CAZ3RMWTVfXHR6Jp8kGdgH9jbqKb83kKbhfW5TeLZ --auth-id 45 --upload-mode=true --repo-id Altisec-Technologies/CICD-test --branch main

      # 9. Scan image
      #- name: Scan Docker image with Cortex
       # run: cortex scan image demo-cicd-app:latest
      
      # 10. Scan Code
      #- name: Scan Code
       # run: ./cortexcli --api-base-url https://api-atpl-nfr.xdr.in.paloaltonetworks.com --api-key NrQivSxdJwfgerY61f61cErkGpUA1dAUECToHiU2xsxfCfjiRmDEGraLFh5dHOSTYfsgyzuNSehmS2uKSpzM3i7CAZ3RMWTVfXHR6Jp8kGdgH9jbqKb83kKbhfW5TeLZ --api-key-id 45 code scan -h


        #Above is the custom code
#########################################################################################################################################
        #Below is the code provided by Cortex during Github Actions integration

        
name: Cortex CLI Code Scan

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  CORTEX_API_KEY: ${{secrets.CORTEX_API_KEY}}
  CORTEX_API_KEY_ID: ${{secrets.CORTEX_API_KEY_ID}}
  CORTEX_API_URL: https://api-atpl-nfr.xdr.in.paloaltonetworks.com
  
jobs:
  cortex-code-scan:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 22

    - name: Verify Node.js Version
      run: node -v

    - name: Download cortexcli
      run: |
        set -x
        crtx_resp=$(curl "${CORTEX_API_URL}/public_api/v1/unified-cli/releases/download-link?os=linux&architecture=amd64" \
          -H "x-xdr-auth-id: ${CORTEX_API_KEY_ID}" \
          -H "Authorization: ${CORTEX_API_KEY}")
        crtx_url=$(echo $crtx_resp | jq -r ".signed_url")
        curl -o cortexcli $crtx_url
        chmod +x cortexcli
        ./cortexcli --version

    - name: Run Cortex CLI Code Scan
      run: |
        ./cortexcli \
          --api-base-url "${CORTEX_API_URL}" \
          --api-key "${CORTEX_API_KEY}" \
          --api-key-id "${CORTEX_API_KEY_ID}" \
          code scan \
          --directory "${{github.workspace}}" \
          --repo-id "${{github.repository}}" \
          --branch "${{github.ref_name}}" \
          --source "GITHUB_ACTIONS" \
          --create-repo-if-missing
