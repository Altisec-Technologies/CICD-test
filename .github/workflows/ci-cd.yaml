name: CI/CD with Cortex

on:
  push:
    branches:
      - main

jobs:
  build-test-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # Node.js setup
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      # Docker build
      - name: Build Docker image
        run: docker build -t demo-cicd-app .

      # Cortex integration
      - name: Install Cortex CLI
        run: curl -sSL https://install.cortex.io | bash

      - name: Cortex login
        run: cortex login --api-key ${{ secrets.CORTEX_API_KEY }}

      - name: Scan Docker image with Cortex
        run: cortex scan image demo-cicd-app:latest